{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://runegoblin.dev/mosh/common.schema.json",
  "title": "Shared value types for the rescued tier",
  "$defs": {
    "contentId": {
      "description": "Stable key into content/ids.json. Minted once, never re-derived; a rename must not move it.",
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "foundryId": { "type": "string", "pattern": "^[A-Za-z0-9]{16}$" },
    "html": {
      "description": "Enriched HTML as Foundry stores it, @UUID links included.",
      "type": "string"
    },
    "img": { "type": "string", "pattern": "^systems/mothershiprpg/" },
    "rollRange": {
      "type": "array",
      "items": { "type": "integer", "minimum": 0 },
      "minItems": 2,
      "maxItems": 2
    },
    "modifiers": {
      "description": "Gameplay data, not metadata: the roll pipeline reads these as [+] / [-].",
      "type": "array",
      "items": { "enum": ["advantage", "disadvantage"] },
      "uniqueItems": true
    }
  }
}
