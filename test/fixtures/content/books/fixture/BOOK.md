# Fixture book

Not a book. Three datasets — one per document type, cross-linked by `@UUID` the way real content
is — so `test/content-pipeline.test.ts` exercises the pipeline without waiting on the PSG
transcription. Its layout mirrors `content/books/psg/` exactly, which is the point.
